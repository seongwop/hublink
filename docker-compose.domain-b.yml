# domain-b VM 서비스 묶음
services:
  # 주문 서비스
  order-service:
    image: ${IMAGE_REGISTRY}/hublink-order-service:${ORDER_SERVICE_IMAGE_TAG:-latest}
    container_name: hublink-order-service
    restart: on-failure
    env_file:
      - ${HUBLINK_ENV_FILE:-.env.gcp}
    environment:
      SERVER_PORT: "19094"
      SPRING_CONFIG_IMPORT: configserver:${CONFIG_SERVER_URL}
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: ${EUREKA_URL}
      EUREKA_INSTANCE_HOSTNAME: ${DOMAIN_B_VM_IP}
      EUREKA_INSTANCE_PREFER_IP_ADDRESS: "true"
      EUREKA_INSTANCE_IP_ADDRESS: ${DOMAIN_B_VM_IP}
      EUREKA_INSTANCE_NON_SECURE_PORT: "19094"
    ports:
      - "19094:19094"

  # 재고 서비스
  stock-service:
    image: ${IMAGE_REGISTRY}/hublink-stock-service:${STOCK_SERVICE_IMAGE_TAG:-latest}
    container_name: hublink-stock-service
    restart: on-failure
    env_file:
      - ${HUBLINK_ENV_FILE:-.env.gcp}
    environment:
      SERVER_PORT: "19098"
      SPRING_CONFIG_IMPORT: configserver:${CONFIG_SERVER_URL}
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: ${EUREKA_URL}
      EUREKA_INSTANCE_HOSTNAME: ${DOMAIN_B_VM_IP}
      EUREKA_INSTANCE_PREFER_IP_ADDRESS: "true"
      EUREKA_INSTANCE_IP_ADDRESS: ${DOMAIN_B_VM_IP}
      EUREKA_INSTANCE_NON_SECURE_PORT: "19098"
    ports:
      - "19098:19098"

  # 배송 서비스
  delivery-service:
    image: ${IMAGE_REGISTRY}/hublink-delivery-service:${DELIVERY_SERVICE_IMAGE_TAG:-latest}
    container_name: hublink-delivery-service
    restart: on-failure
    env_file:
      - ${HUBLINK_ENV_FILE:-.env.gcp}
    environment:
      SERVER_PORT: "19099"
      SPRING_CONFIG_IMPORT: configserver:${CONFIG_SERVER_URL}
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: ${EUREKA_URL}
      EUREKA_INSTANCE_HOSTNAME: ${DOMAIN_B_VM_IP}
      EUREKA_INSTANCE_PREFER_IP_ADDRESS: "true"
      EUREKA_INSTANCE_IP_ADDRESS: ${DOMAIN_B_VM_IP}
      EUREKA_INSTANCE_NON_SECURE_PORT: "19099"
    ports:
      - "19099:19099"

  # Slack 알림 서비스
  slack-service:
    image: ${IMAGE_REGISTRY}/hublink-slack-service:${SLACK_SERVICE_IMAGE_TAG:-latest}
    container_name: hublink-slack-service
    restart: on-failure
    env_file:
      - ${HUBLINK_ENV_FILE:-.env.gcp}
    environment:
      SERVER_PORT: "19100"
      SPRING_CONFIG_IMPORT: configserver:${CONFIG_SERVER_URL}
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: ${EUREKA_URL}
      EUREKA_INSTANCE_HOSTNAME: ${DOMAIN_B_VM_IP}
      EUREKA_INSTANCE_PREFER_IP_ADDRESS: "true"
      EUREKA_INSTANCE_IP_ADDRESS: ${DOMAIN_B_VM_IP}
      EUREKA_INSTANCE_NON_SECURE_PORT: "19100"
    ports:
      - "19100:19100"

  # AI 연계 서비스
  ai-service:
    image: ${IMAGE_REGISTRY}/hublink-ai-service:${AI_SERVICE_IMAGE_TAG:-latest}
    container_name: hublink-ai-service
    restart: on-failure
    env_file:
      - ${HUBLINK_ENV_FILE:-.env.gcp}
    environment:
      SERVER_PORT: "19101"
      SPRING_CONFIG_IMPORT: configserver:${CONFIG_SERVER_URL}
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: ${EUREKA_URL}
      EUREKA_INSTANCE_HOSTNAME: ${DOMAIN_B_VM_IP}
      EUREKA_INSTANCE_PREFER_IP_ADDRESS: "true"
      EUREKA_INSTANCE_IP_ADDRESS: ${DOMAIN_B_VM_IP}
      EUREKA_INSTANCE_NON_SECURE_PORT: "19101"
    ports:
      - "19101:19101"

  # domain-b VM Docker 로그 수집기
  promtail:
    image: ${IMAGE_REGISTRY}/hublink-promtail:latest
    container_name: hublink-promtail-domain-b
    restart: unless-stopped
    env_file:
      - ${HUBLINK_ENV_FILE:-.env.gcp}
    environment:
      HUBLINK_VM_ROLE: domain-b
    volumes:
      - ./monitoring/promtail.yml:/etc/promtail/config.yml:ro
      - promtail-positions:/var/lib/promtail
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    command: -config.file=/etc/promtail/config.yml -config.expand-env=true

# 로그 수집 위치 저장 볼륨
volumes:
  promtail-positions:
